export type ListNode = {
  val: number;
  next: ListNode | null;
};

export type TreeNode = {
  val: number;
  left: TreeNode | null;
  right: TreeNode | null;
};

export type _Node = {
  val: number;
  left: _Node | null;
  right: _Node | null;
  next: _Node | null;
};
